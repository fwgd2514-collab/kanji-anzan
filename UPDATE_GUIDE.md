# 6分野・みっくん冒険メニュー版への更新手順

## index.html・index2.html簡単グループ分離版（version 48）について

パスワードやFirebase Authenticationを使わず、入口ファイルだけで2つのグループを分ける方式へ変更しました。

- `index.html`は、これまで通り`names.txt`と`nobiru-family-01`を使用
- `index2.html`は、新しい名前登録式グループ`nobiru-group-02`を使用
- 2つのグループで名前、レベル、XP、最終学習日時、端末内保存領域を分離
- 新グループの名前は起動画面または設定画面から登録
- Authenticationユーザー、UID、メンバー文書、グループ用パスワードは不要

### 更新後のURL

従来グループ：

`https://ユーザー名.github.io/リポジトリ名/?update=48`

新しいグループ：

`https://ユーザー名.github.io/リポジトリ名/index2.html?update=48`

### Firebaseで行う設定

このフォルダの新しい`firestore.rules`を、FirebaseコンソールのFirestore「ルール」へ全文貼り直して「公開」を押します。それ以外のAuthentication設定は不要です。

詳しい操作は`FIREBASE_SETUP.md`を参照してください。

### GitHubへ反映するファイル

次の12ファイルを同じ場所へ上書き・追加してください。

1. `app.js`
2. `styles.css`
3. `firebase-sync.js`
4. `firebase-config.js`
5. `firestore.rules`
6. `index.html`
7. `index2.html`（新規追加）
8. `version.json`
9. `manifest2.webmanifest`（新規追加）
10. `README.md`
11. `UPDATE_GUIDE.md`
12. `FIREBASE_SETUP.md`

`firestore.rules`はGitHubへアップロードするだけではFirebase側へ反映されません。FirebaseコンソールのFirestore「ルール」へも全文を貼り付けて公開してください。

## 漢字難易度・漢字王・みっくんごほうび版（version 46）について

学年の範囲を守りながら通常の漢字問題を一段だけ難しくし、漢字王とみっくん版を大きく強化しました。

- Lv.1〜96の漢字は、10問設定時に現在の学年を約9割、直前範囲の復習を約1割に変更
- 小学低学年から熟語を少しずつ増やし、小学3年以降と中学では熟語問題を従来より多く出題
- 学年後半では読みの長さや画数が近い選択肢を優先し、迷いやすい4択へ変更
- 漢字王を220字へ拡大し、「魑魅魍魎」「躊躇」「鸚鵡」「齷齪」などの難読語で使う難字を先頭に追加
- 漢字王の難読漢字を開くと、その難読語の読み方と意味を表示
- iPhone 15 Pro Maxを含むスマホ幅で、「きょうのトレーニング」と「問題数を変更」を1行表示
- みっくんホームに、毎日内容とごほうびが変わる「びっくりミッション」を追加
- みっくん学習中に、プレイごとに変わる「ひみつミッション」と連続正解数を表示
- みっくんの5問終了後に、正解率に応じたごほうびカードと最高連続正解数を表示

### 更新URL

`https://ユーザー名.github.io/リポジトリ名/?update=46`

画面に「最新版に更新」が表示されている端末では、そのボタンでも更新できます。

### GitHubへ反映するファイル

次の7ファイルを同じ場所へ上書きしてください。

1. `app.js`
2. `styles.css`
3. `index.html`
4. `version.json`
5. `README.md`
6. `UPDATE_GUIDE.md`
7. `DATA_SOURCES.md`

今回は画像・Firebaseルール・`names.txt`・漢字データファイルの変更はありません。

## 楽しさ・カード48種類・漢字説明改善版（version 45）について

学習中の見やすさを直し、毎日もう一度開きたくなる演出と新しい絵を追加しました。

- 書き順終了後の「書き順を確認できました」を漢字枠の外へ移動
- 完成した漢字に文字が重ならず、最後まで全体を確認可能
- 「安泰」は意味に加えて、「家族の暮らしは安泰だ」という使い方まで説明
- 意味データがない言葉も、観察・音読につながる丁寧な学習案内へ変更
- ふくろう・りす・はりねずみ・かえる・とら・うし・ひつじ・うま・ケーキ・虹・時計・ロケット・本・太鼓・宝箱・王冠の絵を追加
- フラッシュカードは従来の32種類から48種類へ増加
- ホームに、日付と学習者ごとに毎日変わる「TODAY'S QUEST」を追加
- 正解時の言葉を数種類へ増やし、きらめき演出を追加

### 更新URL

`https://ユーザー名.github.io/リポジトリ名/?update=45`

画面に「最新版に更新」が表示されている端末では、そのボタンでも更新できます。

### GitHubへ反映するファイル

次の7ファイルを同じ場所へ上書きしてください。

1. `app.js`
2. `styles.css`
3. `index.html`
4. `version.json`
5. `README.md`
6. `UPDATE_GUIDE.md`
7. `assets/learning-card-atlas-adventure-v1.png`

今回は新しい画像ファイルも必要です。`assets`フォルダ内へ、ファイル名を変更せずアップロードしてください。Firebaseルール、`names.txt`、漢字データファイルの変更はありません。

## お受験の並び替え・漢字王・書き順上部表示版（version 44）について

漢字一覧の難易度順と、漢字を開いた時の書き順の位置を調整しました。

- 「お受験」を小学6年と中学1年の間へ移動
- お受験は中学受験向けの発展一覧として、難関四字熟語で使われる277字を表示
- 中学3年の後に、高校卒業以上の挑戦枠「漢字王」を追加
- 漢字王は、画数の多さと使用頻度の低さから選んだクイズ番組級の難漢字180字
- 漢字をタップした直後、読み方や活用事例より先に書き順アニメーションを表示
- 学習レベル内部の小学・中学の進行順は変更せず、一覧表示だけを安全に並び替え

### 更新URL

`https://ユーザー名.github.io/リポジトリ名/?update=44`

画面に「最新版に更新」が表示されている端末では、そのボタンでも更新できます。

### GitHubへ反映するファイル

次の6ファイルを同じ場所へ上書きしてください。

1. `app.js`
2. `styles.css`
3. `index.html`
4. `version.json`
5. `README.md`
6. `UPDATE_GUIDE.md`

Firebaseルール、`names.txt`、漢字データファイルの変更はありません。

## 漢字詳細学習・お受験・前回ユーザー版（version 43）について

学年別漢字一覧をタップして学べる画面と、前回の学習者で素早く始める操作を追加しました。

- 一覧の漢字を押すと、その漢字専用の詳細画面を表示
- 詳細画面で、主な読み方・複数の読み方・ことば・活用事例を確認
- 既存のKanjiVG書き順機能を使い、一画ずつのアニメーションと「もう一度」を表示
- 中学3年の右側に「お受験」を追加
- お受験は学年配当ではなく、難関レベルの四字熟語で使われる277字を集めた発展一覧
- 起動時に前回の学習者が名簿に残っていれば、「この人で続ける」を大きく表示
- 同じ人なら名前の一覧を開かずに開始でき、別の人だけ名前を選び直す方式
- これまでの名前・6分野レベル・XP・学習履歴・Firebase同期データはそのまま引き継ぎ

### 更新URL

`https://ユーザー名.github.io/リポジトリ名/?update=43`

version 42以降が表示されている端末では、画面の「最新版に更新」ボタンでも更新できます。

### GitHubへ反映するファイル

次の6ファイルを同じ場所へ上書きしてください。

1. `app.js`
2. `styles.css`
3. `index.html`
4. `version.json`
5. `README.md`
6. `UPDATE_GUIDE.md`

Firebaseルール、`names.txt`、漢字データファイルの変更はありません。

## 「最新版に更新」ボタン版（version 42）について

iPhoneで古い画面が残る場合に、利用者自身で最新ファイルを読み直せるボタンを追加しました。

- 名前を選ぶ最初の画面に「最新版に更新」を追加
- ホーム画面上部にも「最新版」ボタンを追加
- ボタンを押すと最新の`version.json`をキャッシュを使わず確認
- 毎回異なる再読込用URLへ移動し、iPhoneが以前の画面を再利用しにくい方式
- 更新中はボタンを「確認中…」に変えて、連続タップを防止
- `localStorage`やFirebaseの学習記録は削除しないため、名前・レベル・XP・学習履歴を保持
- 通常の自動更新確認も引き続き動作

### 最初の1回だけ

この更新ボタン自体をiPhoneへ読み込むため、version 42への更新時だけ次のURLで開いてください。

`https://ユーザー名.github.io/リポジトリ名/?update=42`

version 42が表示された後は、次回から画面上の「最新版に更新」ボタンを使用できます。

### GitHubへ反映するファイル

次の6ファイルを同じ場所へ上書きしてください。

1. `app.js`
2. `styles.css`
3. `index.html`
4. `version.json`
5. `README.md`
6. `UPDATE_GUIDE.md`

Firebaseルールの変更はありません。現在の名前、レベル、XP、学習履歴はそのまま引き継がれます。

## iPhone答え合わせ表示・回答確定版（version 41）について

version 40のフラッシュカード答え合わせ表示と、フラッシュ暗算の誤答後操作を修正しました。

- フラッシュカードの比較欄で、枠のスタイルが内側の絵まで覆っていた問題を修正
- 「あなたが入れた順番」「正しい順番」の絵カードを省略せず表示
- 2〜7枚のカードをiPhoneの横幅に合わせて均等配置し、右側が切れないよう調整
- 正解時のカード確認欄にも同じ画面幅対応を適用
- フラッシュ暗算は最初の「こたえる」で正誤を確定
- 誤答後は数字キーと削除キーを無効にし、追加回答できないよう変更
- 誤答後は正解と出題された数字を確認し、「結果を確認したら次へ」で進行

### GitHubへ反映するファイル

次の6ファイルを同じ場所へ上書きしてください。

1. `app.js`
2. `styles.css`
3. `index.html`
4. `version.json`
5. `README.md`
6. `UPDATE_GUIDE.md`

アップロード後、次のURLで確認します。

`https://ユーザー名.github.io/リポジトリ名/?update=41`

Firebaseルールの変更はありません。現在の名前、レベル、XP、学習履歴はそのまま引き継がれます。

## 答え合わせ・学年別漢字一覧版（version 40）について

答え合わせを分かりやすくし、iPhone上部の操作性と漢字の確認機能を改善しました。

- フラッシュカードで間違えた後、「あなたが入れた順番」と「正しい順番」を上下2段で表示
- 違っていた位置は赤、合っていた位置は緑の枠で確認
- フラッシュ暗算で間違えた後、実際に出題された数字を「数字＋数字」の形で表示
- フラッシュ暗算の出題数字は、回答を直している間も確認可能
- 学習画面上部にiPhoneの安全領域分の余白を追加し、「やめる」と時刻表示の重なりを防止
- ホームに「学年別漢字一覧」を追加
- 小学1〜6年は学年別漢字配当表の1,026字を学年ごとに表示
- 中学1〜3年は、小学校配当外の常用漢字1,110字をアプリの学習順で400字・400字・310字に分けて表示
- 漢字の下に代表的な読み方を併記し、学年タブは横に動かして切り替え可能
- 名前選択画面の最終更新日を2026年8月8日に更新

### GitHubへ反映するファイル

次の6ファイルを同じ場所へ上書きしてください。

1. `app.js`
2. `styles.css`
3. `index.html`
4. `version.json`
5. `README.md`
6. `UPDATE_GUIDE.md`

アップロード後、次のURLで確認します。

`https://ユーザー名.github.io/リポジトリ名/?update=40`

Firebaseルールの変更はありません。現在の名前、レベル、XP、学習履歴はそのまま引き継がれます。

## だんだんだん・操作ボタン自動表示版（version 39）について

「だんだんだんをうごかそう」で、上下左右ボタンが画面の下に隠れる場合の表示を改善しました。

- ミッションを開いた時、上下左右ボタン全体が見える位置へ自動スクロール
- 1マス移動して画面を更新した後も、必要な場合だけ操作ボタンへ自動スクロール
- ボタンがすでに全て見えている場合はスクロールせず、画面の不要な揺れを防止
- 上下左右ボタンの大きさはversion 38のまま変更していません
- iPhoneの表示可能範囲を基準にして、下部ブラウザ表示の内側へ収まるよう調整

### GitHubへ反映するファイル

次の6ファイルを同じ場所へ上書きしてください。

1. `app.js`
2. `styles.css`
3. `index.html`
4. `version.json`
5. `README.md`
6. `UPDATE_GUIDE.md`

アップロード後、次のURLで確認します。

`https://ユーザー名.github.io/リポジトリ名/?update=39`

Firebaseルールの変更はありません。現在の名前、レベル、XP、学習履歴はそのまま引き継がれます。

## だんだんだん・連続移動ゲーム版（version 38）について

「だんだんだんをうごかそう」を、1回の方向選択で終わる問題から、1つのマップ内を最後まで進む移動ゲームへ変更しました。

- 上下左右の大きな操作ボタンを、到着まで何度でも押せます
- 黄色い道を見ながら途中で方向を変え、16マス版は6回、20マス版は7回進みます
- 岩・木・水などがある方向を押しても問題は終了せず、その場所からやり直せます
- 進んだ道にチェックを付け、現在位置と残りマス数を表示します
- お宝へ着いた時だけ正解として記録し、レベル・XPへ反映します
- 到着演出の後は「つぎのミッションへ」が見える位置まで自動スクロールします
- 「つぎのミッションへ」を通常配置にし、iPhoneの下部ブラウザ表示との重なりを防ぎます

### GitHubへ反映するファイル

次の6ファイルを同じ場所へ上書きしてください。

1. `app.js`
2. `styles.css`
3. `index.html`
4. `version.json`
5. `README.md`
6. `UPDATE_GUIDE.md`

アップロード後、次のURLで確認します。

`https://ユーザー名.github.io/リポジトリ名/?update=38`

Firebaseルールの変更はありません。現在の名前、レベル、XP、学習履歴はそのまま引き継がれます。

## メカづくり・障害物マップ改善版（version 37）について

みっくん用ミニゲームの画面バランスと内容を改善しました。

- 「メカをつくろう」で、正解時に選んだ絵とは違う部品が表示される問題を修正
- 正解した実際の絵を、5個の部品トレーへ順番に追加
- 部品トレーを固定サイズの5列にし、背景の四角枠のずれを修正
- 「大きなメカはどれ？」などの問題文を、大きく太い見出しへ変更
- 「だんだんだんをうごかそう」を、単純な9マス風画面から障害物マップへ変更
- 年少さん段階は4×4の16マス
- 年少さん・ぐんぐん以降は5×4の20マス
- 黄色い道を進み、岩・木・水・丸太などを避けてお宝を目指す画面
- 正解すると、だんだんだんが正しい方向へ1マス動く

### GitHubへ反映するファイル

次の6ファイルを同じ場所へ上書きしてください。

1. `app.js`
2. `styles.css`
3. `index.html`
4. `version.json`
5. `README.md`
6. `UPDATE_GUIDE.md`

アップロード後、次のURLで確認します。

`https://ユーザー名.github.io/リポジトリ名/?update=37`

Firebaseルールの変更はありません。現在の名前、レベル、XP、学習履歴はそのまま引き継がれます。

## みっくん・ミニゲーム刷新版（version 36）について

みっくん専用メニューを、問題集のような三択中心の見せ方から、絵・光・動きで遊ぶ短いミッションへ刷新しました。先にご連絡いただいた操作上の3点も同時に修正しています。

- 「メカをつくろう」：正しいパーツを選ぶと、5問でメカの部品が埋まる
- 「だんだんだんをうごかそう」：上下左右の矢印を選び、正解するとお宝まで動く
- 「おたからをあつめよう」：メカパーツやお宝を数えて集める
- 「ピカッときおく」：絵が1枚ずつ光って消え、最後の絵を答える
- 全メニューを1回5問の短いミッションに統一
- 正解時は部品・お宝・光のパワーを獲得する演出を表示
- みっくん用で間違えてもレベルは下げず、正しい答えを見せて次へ進む
- レベル調整パスワードの入力欄を押すと元画面へ戻る問題を修正
- 全コースの正解表示を、大きな緑色のチェック付き表示へ統一
- 漢字回答後は正解表示と学習ポイントが続けて見える位置へスクロール
- フラッシュカード回答後は、正しい順番と結果が見える位置へ自動スクロール
- フラッシュカードの「次へ」を通常配置にし、結果表示との重なりを解消

### GitHubへ反映するファイル

次の6ファイルを同じ場所へ上書きしてください。

1. `app.js`
2. `styles.css`
3. `index.html`
4. `version.json`
5. `README.md`
6. `UPDATE_GUIDE.md`

アップロード後、次のURLで確認します。

`https://ユーザー名.github.io/リポジトリ名/?update=36`

パスワードは引き続き `1234` です。Firebaseルールの変更はなく、現在の名前、分野別レベル、XP、学習履歴はそのまま引き継がれます。

## 操作ロック・表示安定版（version 35）について

今回の更新内容は次のとおりです。

- 名前選択画面の下に「最終更新：2026年8月6日」を表示
- 分野別レベル調整を開くたびに、保護者用パスワードを要求
- 初期パスワードは `1234`
- 学習中の「やめる」ボタンを大きくし、左端から少し内側へ移動
- 数字記憶は、2問目以降「結果を確認したら次へ」の後に開始ボタンを出さず、横バーから自動開始
- 3問・5問ごとの途中確認後も、数字記憶を横バーから自動開始
- フラッシュカードは画面全体を毎回描き直さず、カード部分だけを一定間隔で更新
- カード画像を先読みし、iPhoneでの表示遅れやカクつきを軽減
- 漢字回答後の「この漢字の学習ポイント」の説明文を大きく表示

### GitHubへ反映するファイル

GitHubのリポジトリを開き、次の6ファイルを同じ場所へ上書きしてください。

1. `app.js`
2. `styles.css`
3. `index.html`
4. `version.json`
5. `README.md`
6. `UPDATE_GUIDE.md`

アップロード後、GitHub Pagesを次のように開きます。

`https://ユーザー名.github.io/リポジトリ名/?update=35`

iPhoneではSafariをいったん閉じて開き直し、上記URLで確認してください。通常は `version.json` が新しい版を検出して自動更新しますが、URLの `?update=35` を変えると確認が確実です。

### パスワードについて

初期パスワードは `1234` です。変更する場合は、`app.js` の先頭付近にある次の行の数字だけを変更してください。

`const LEVEL_ADJUSTMENT_PASSWORD = "1234";`

GitHub Pagesは静的サイトのため、ソースを調べればパスワードを確認できます。この機能は強固な本人認証ではなく、子どもが誤って分野別レベルを変更することを防ぐための簡易ロックです。

version 35ではFirebaseルールの変更はありません。既存の名前、分野別レベル、XP、学習履歴はそのまま引き継がれます。

## 学年別漢字カリキュラム版（version 34）について

小学生・中学生が、その学年で学ぶべき漢字を順に身につけられる出題方式へ変更しました。

- 小学1年80字、小学2年160字、小学3年200字、小学4年202字、小学5年193字、小学6年191字を収録（合計1,026字）
- 中学校では小学校配当外の常用漢字1,110字を、中1〜中3で段階的に出題
- 各学年の最初は基本的で画数の少ない漢字から始め、レベルが上がるほど出題範囲を拡大
- 現在の学年を約8割、直前範囲の復習を約2割出題
- 学年後半では単漢字に加えて熟語の読み・穴埋めが増え、自然に難易度が上昇
- Lv.97〜120は従来の高校入試・大学・天才向け四字熟語問題を継続
- 同じ10問内で同じ漢字・同じ熟語を重ねず、同じ人の直近160字を避けて出題
- ホームと問題画面に「小学3年配当 84/200字」のような出題範囲を表示

### GitHubへ反映するファイル

GitHubのリポジトリを開き、次の8ファイルを同じ場所へ上書きまたは追加してください。

1. `app.js`
2. `styles.css`
3. `index.html`
4. `version.json`
5. `kanji-curriculum-data.js`（新規追加）
6. `README.md`
7. `UPDATE_GUIDE.md`
8. `DATA_SOURCES.md`

アップロード後、GitHub Pagesを次のように開きます。

`https://ユーザー名.github.io/リポジトリ名/?update=34`

version 34ではFirebaseルールの変更はありません。既存の名前別レベルと学習履歴はそのまま引き継がれます。

## お気に入り・ホーム画面アイコン版（version 33）について

灰色地に白い「の」だった標準アイコンを、濃紺・珊瑚色・金色を使った専用アイコンへ変更しました。本から成長の光が伸びるデザインで、学習とレベルアップを表しています。

- Safari・Chromeなどのブラウザタブ用アイコン
- iPhoneの「ホーム画面に追加」用180pxアイコン
- Android・Webアプリ用192px／512pxアイコン
- ホーム画面で表示されるアプリ名「のびる」
- 濃紺のテーマカラー

### GitHubへ反映するファイル

次のファイルを同じ場所へ上書きまたは追加してください。

1. `index.html`
2. `version.json`
3. `manifest.webmanifest`（新規追加）
4. `README.md`
5. `UPDATE_GUIDE.md`
6. `assets/favicon-v1.ico`（新規追加）
7. `assets/favicon-32-v1.png`（新規追加）
8. `assets/favicon-48-v1.png`（新規追加）
9. `assets/apple-touch-icon-v1.png`（新規追加）
10. `assets/app-icon-192-v1.png`（新規追加）
11. `assets/app-icon-512-v1.png`（新規追加）

アップロード後、GitHub Pagesを次のように開きます。

`https://ユーザー名.github.io/リポジトリ名/?update=33`

iPhoneですでにホーム画面へ追加している場合は、古いアイコンを一度削除してから、Safariの共有メニューで「ホーム画面に追加」をやり直してください。Safariのお気に入りだけの場合も、古いキャッシュが残る時は一度削除して登録し直すと確実です。

version 33ではFirebaseルールの変更はありません。

## みっくん専用64問・漢字回答後スクロール版（version 32）について

今回の更新内容は次のとおりです。

- みっくんメニューで、最初の1問だけ新しく残りが以前の問題になる状態を解消
- 4メニューを新しい専用問題へ完全に切り替え、各16問・合計64問を収録
- 最初の「年少さん」段階にも各メニュー8問を用意し、5問コース内で同じ問題を重ねにくく調整
- 表示名を「あんぱんのヒーロー」と「だんだんだん」に変更
- ヒーロー、メカ、あんぱん、町、メカ部品、ランプなど、みっくん専用の絵を16種類追加
- 漢字の回答後は、熟語の意味と書き順ボタンが見える位置まで自動スクロール
- 漢字画面の「次へ」ボタンを通常配置にし、意味・書き順ボタンとの重なりを解消

### GitHubへ反映するファイル

GitHubのリポジトリを開き、次の7ファイルを同じ場所へ上書きまたは追加してください。

1. `app.js`
2. `styles.css`
3. `index.html`
4. `version.json`
5. `README.md`
6. `UPDATE_GUIDE.md`
7. `assets/mikkun-adventure-atlas-v1.png`（新規追加）

アップロード後、GitHub Pagesの画面を開き、URL末尾へ `?update=32` を付けて確認します。例：

`https://ユーザー名.github.io/リポジトリ名/?update=32`

version 32ではFirebaseルールの変更はありません。version 31のLv.120対応ルールをまだ公開していない場合だけ、続くversion 31の手順で `firestore.rules` を公開してください。

## 結果確認・意味と書き順・Lv.120対応版（version 31）について

今回の更新内容は次のとおりです。

- 数字記憶、フラッシュカード、フラッシュ暗算、暗算、漢字を読む、漢字を選ぶの全コースで、自動的に次へ進む処理を廃止
- 正解・不正解と正しい答えを画面に残し、本人が「次へ」を押してから進行
- フラッシュカードは選んだ各位置の正誤と、正しい絵・順番・名前を並べて確認可能
- 「漢字を読む」「漢字を選ぶ」は、回答後に熟語の短い意味を表示
- 回答後に熟語内の漢字を選ぶと、既存のKanjiVGアニメーションで書き順を確認可能
- 既存のLv.1〜100は変更せず、Lv.101〜110に「大学レベル」、Lv.111〜120に「天才レベル」を追加
- レベル調整、端末内保存、Firebase同期をLv.120まで対応
- フラッシュ暗算、フラッシュカード、数字記憶の開始・表示速度をレベル別に段階調整
- みっくんメニューへ、パンヒーローとオリジナルの「だだんメカ」を題材にした絵・パーツ・数・並び問題を追加
- iPhoneで結果確認中も「次へ」を見つけやすいよう、下部の安全領域を避けてボタンを表示

### GitHubへ反映するファイル

GitHubのリポジトリを開き、次の10ファイルを同じ場所へ上書きアップロードしてください。

1. `app.js`
2. `styles.css`
3. `question-data.js`
4. `firebase-sync.js`
5. `firestore.rules`
6. `index.html`
7. `version.json`
8. `README.md`
9. `UPDATE_GUIDE.md`
10. `DATA_SOURCES.md`

アップロード後、GitHub Pagesの画面を開き、URL末尾へ `?update=31` を付けて確認します。例：

`https://ユーザー名.github.io/リポジトリ名/?update=31`

### Firebaseルールの再公開（今回は必要）

Lv.101〜120を別端末へ保存できるよう、Firestoreの上限を100から120へ変更しています。GitHubへのアップロードだけではFirebaseルールは変わらないため、次の操作も行ってください。

1. Firebaseコンソールで対象プロジェクトを開く
2. 左側の `Firestore` を開く
3. 上部の `ルール` タブを開く
4. ローカルの `firestore.rules` をメモ帳で開く
5. Firebase画面に表示されているルールをすべて選択して削除
6. `firestore.rules` の内容を先頭から最後まで貼り付ける
7. `公開` を押す
8. 数分後、アプリの設定画面で「今すぐ同期」を押す

古いルールのままだとLv.100までは使えますが、Lv.101以上になった時のFirebase保存だけが拒否されます。端末内のレベルが消えるわけではありません。

## 漢字重複防止・ボタン配置・音質改善版（version 30）について

今回の更新内容は次のとおりです。

- 「漢字を選ぶ」は穴の位置が違っても同じ四字熟語として扱い、1回の学習内では重ねて出題しない
- 四字熟語を80語から400語へ増やし、各難易度帯に40語ずつ収録
- 同じ人の直近30熟語を避け、通常の10問設定では同じ難易度帯を4回学習するまで再登場を抑制
- 通常の10問では10種類すべて別の四字熟語を出題
- 5問・10問終了後の「ホームへ戻る」を成績のすぐ下へ移動
- 区切り画面をiPhoneのSafari下部バーより上へ持ち上げ、縦が短い場合は画面内でスクロール可能に変更
- レベル微調整欄の「Lv.＊＊から始める」を「レベル調整を終える」へ変更し、最下段の同名ボタンを削除
- 効果音に音の重なり・余韻・音量調整を加えて質感を改善
- 操作・開始・正解・不正解・レベルアップの5種類へ増加
- 設定画面で5種類の音を個別に試聴可能

version 18で最新の `firestore.rules` を公開済みの場合、version 30ではFirebaseルールの再公開は不要です。今回は `app.js`、`styles.css`、`question-data.js`、`index.html`、`version.json`、`README.md`、`UPDATE_GUIDE.md`、`DATA_SOURCES.md` をGitHubへ上書きアップロードしてください。

## 名前確認必須・絵だけ記憶版（version 29）について

今回の更新内容は次のとおりです。

- アプリを開くたびに、最初に「自分の名前を選んでください」を表示
- 名前欄は未選択から始まり、名前を明示的に選ぶまで開始ボタンを押せない
- 名前を確定するまでホームや学習メニューを表示しない
- 選んだ名前のレベル・XP・学習日時へ、これまでどおり保存・Firebase同期
- フラッシュカードの表示中は、絵の下に動物名・物の名前を表示しない
- 回答候補からも名前を隠し、絵そのものを見て順番を回答
- 正誤判定後だけ、正しい順番を絵文字と名前で確認可能

version 18で最新の `firestore.rules` を公開済みの場合、version 29ではFirebaseルールの再公開は不要です。今回は `app.js`、`styles.css`、`index.html`、`version.json`、`README.md`、`UPDATE_GUIDE.md` をGitHubへ上書きアップロードしてください。

## 漢字選択・熟語強化版（version 28）について

今回の更新内容は次のとおりです。

- 通常モードの「漢字を書く」を「漢字を選ぶ」へ変更
- 四字熟語の1文字を隠す問題と、レベルが上がると2文字を隠す問題を追加
- 隠れた漢字が2文字の場合は、正しい並びをまとめて4択から回答
- 四字熟語80語から難易度別に出題し、同じ問題の連続を抑制
- 「漢字を読む」は単漢字の追加出題をやめ、二字以上の熟語と四字熟語を中心に強化
- 四字熟語の読み問題を80語追加
- 既存の分野別レベル・XP・Firebase同期データはそのまま引き継ぎ
- `names.txt` の「みっくん」は、これまでどおり専用の冒険メニューを表示

version 18で最新の `firestore.rules` を公開済みの場合、version 28ではFirebaseルールの再公開は不要です。今回は `app.js`、`styles.css`、`question-data.js`、`index.html`、`version.json`、`README.md`、`UPDATE_GUIDE.md` をGitHubへ上書きアップロードしてください。

## 漢字の手書き・書き順改善版（version 27）について

今回の更新内容は次のとおりです。

- 漢字を手書きしている間はページの上下スクロールを止め、iPhoneで書きやすく改善
- 書き順アニメーションから各画の丸数字を削除
- 一画ごとの描画を少しゆっくりに変更
- 形チェック後のボタンを「書き直す／次へ／書き順」へ変更
- 書き順画面の再生ボタンを「もう一度」へ変更
- 「書き順を確認できました」と表示されても、書き順の線が見えなかった問題を修正
- SVGの非表示属性を確実に解除し、iPhoneとPCの両方で書き順を表示
- 漢字を書くで「書き順」を押すと、手書きした文字を消して書き順画面へ移動
- お手本を一画ずつ動かして表示
- 「もう一度」で書き順を再生し、「次の問題へ」で学習を続行
- 書き順データを読み込めない場合も、「次の問題へ」で止まらず進行可能
- 日本の学校書体・書き順に対応したKanjiVGを利用し、画面内に出典とライセンスを表示
- 通常ユーザーが正解してレベルアップした時に、回答後の画面更新が止まる問題を修正
- 数字記憶の「こたえる」、フラッシュカードの最終選択、暗算などの回答後に、自動で次の問題へ進むことを再確認
- みっくんメニューのお題を大きくし、回答候補との間隔を広げて画面バランスを改善
- みっくんのレベルを「年少さん・はじめ」から「年中さん・チャレンジ」まで4段階で表示
- みっくんのレベル一覧・開始位置調整も、同じ4段階の冒険ステップ表示へ変更
- 通常モードを「数字記憶、フラッシュカード、フラッシュ暗算、暗算、漢字を読む、漢字を書く」の順へ変更
- 動物・海の生き物・果物・乗り物を16種類追加し、絵カードを合計32種類へ拡充
- 漢字を書く画面に、文字の大きさ・中心・形のまとまり・画数を確認する「形チェック」を追加
- 形チェック後は「書き直す／次へ／書き順」を選択可能
- みっくんメニューを、文字中心ではない4つのミニゲームへ刷新
  - おたからさがし：お題と同じ絵を見つける
  - なかまさがし：食べ物・乗り物・動物などを見分ける
  - かずのたからばこ：1〜5個の絵を数える
  - つぎはどっち？：絵や色の並びの続きを考える
- 5問の星集めと冒険ゲージを追加
- 通常フラッシュカードを、統一した絵本タッチの絵へ変更
- 操作音を短く澄んだ音、正解音を余韻のあるベル調へ変更
- 数字記憶の「こたえる」ボタンを、iPhoneのSafari下部バーより上に固定

version 18で最新の `firestore.rules` を公開済みの場合、version 27ではFirebaseルールの再公開は不要です。今回は `app.js`、`styles.css`、`index.html`、`version.json`、`README.md`、`UPDATE_GUIDE.md` をGitHubへ上書きアップロードしてください。

## 学習日時・順番記憶・難易度改善版（version 18）について

今回の更新では、次の改善も含まれています。

- 登録者ごとの最終学習日時を一覧表示し、Firebaseで端末間共有
- フラッシュカードを、表示されたすべてのカードを順番どおり選ぶ形式へ変更
- 分野別レベル調整は保存後も閉じず、ほかの分野を続けて変更可能
- 全分野の問題を約1学年分やさしい復習帯へ調整
- 通常操作は「ピッ」、正解時だけ「ピンポーン」を再生
- 数字記憶の回答欄を横長にし、長い数字は画面幅に合わせて縮小表示
- 漢字の読み・動物・色・ABCの回答候補を大きくし、濁点と半濁点を見分けやすく調整
- 通常メニューへ「フラッシュカード」を追加
- 通常メニューへ「数字記憶」を追加
- フラッシュカードは、順番に表示された絵を覚えて4択で回答
- 数字記憶は、右から左へ流れた数字を同じ順番で入力
- レベルが上がるほど、カード枚数・数字の桁数・表示速度が難しくなる
- みっくんメニューを年中向けに追加（version 19で冒険ミニゲームへ再刷新）
- 新しい2分野を含む6分野のレベルとXPを個別保存
- 正解後はボタンを押さず、自動で次の問題へ移動
- 学習画面の「やめる」から、今回分をノーカウントにして終了
- iPhoneの下部ブラウザバーを避ける表示調整
- 暗算の「こたえる」ボタンを画面下部へ固定
- 名前選択欄の直下に「この名前で学習する」を追加
- 名前・問題数・間隔を保存したら、自動でホームへ戻る
- ホーム画面の最上段に「きょうのトレーニング」を表示
- 名前、総合レベル、6分野レベルをトレーニングの後へ整理
- `names.txt` の「みっくん」を選ぶと、年中向けの「みっくんメニュー」を表示
- version 18時点のみっくんメニューは、あいうえお・動物の名前・色当て・ABCを各5問（version 19で冒険ミニゲームへ変更）
- ボタンや数字キーなどを操作すると、短い「ピ」音を再生
- 漢字を書く画面で、下方向へ書いても画面全体が動かないように調整
- 暗算は1回間違えたら正解を表示し、不正解として自動で次へ移動
- フラッシュ暗算の「3・2・1」を、減っていく横バーへ変更
- フラッシュ暗算の正解後は、次の問題を横バーから自動開始
- 「もう一度見る」は1回までとし、その後は「降参する」を表示
- 誤答時は正解時より小さいXPだけを下げ、境界を越えた時だけレベルダウン
- 漢字の書きには「むずかしかった」を追加し、自己判定で少し調整
- 分野別開始レベルを、学年目安10段階と1レベル単位の両方で調整可能
- iPhoneの最初のタップで音声を開始してから、聞き取りやすい「ピ」音を再生
- 設定画面に「ピ」音を確認するボタンを追加
- 正解・レベル調整直後のFirebase保存を高速化
- アプリを閉じる時にも未送信分を保存
- 設定画面に最終同期時刻を表示

version 17以前から更新する場合は、最終学習日時の保存項目が増えるため、`firestore.rules` を新しい内容へ貼り替えて再公開してください。version 18のルールを公開済みなら、version 19での再公開は不要です。

この更新では、次の6分野のレベルとXPを別々に保存します。

- 漢字を書く
- 漢字を読む
- 暗算する
- フラッシュ暗算
- フラッシュカード
- 数字記憶

旧版の共通レベルとXPは、更新後の初回起動時に各分野へ引き継がれます。既に4分野版を利用している場合、新しい2分野はレベル1から始まります。必要に応じて設定画面から開始レベルを変更できます。その後は、学習した分野だけが個別に上がります。

## 作業の順番

version 17以前から更新する場合は次の順番で進めます。version 18のルールを公開済みなら、手順1は飛ばして構いません。

1. 必要な場合だけFirebaseのFirestoreルールを更新して再公開
2. GitHubへローカルファイルをアップロード
3. 公開ページで同期を確認

## 1. FirebaseのFirestoreルールを更新

1. [Firebaseコンソール](https://console.firebase.google.com/)を開きます。
2. 使用中のプロジェクト `gakushu` を選びます。
3. 左側の「Database と Storage」から「Firestore」を開きます。
4. 画面上部の「ルール」を押します。
5. このフォルダにある `firestore.rules` をメモ帳で開きます。
6. メモ帳で `Ctrl + A` を押し、続けて `Ctrl + C` を押します。
7. Firebaseのルール編集欄をクリックします。
8. `Ctrl + A` を押して既存の内容をすべて選択します。
9. `Ctrl + V` を押して、新しいルールへ完全に置き換えます。
10. 末尾が `}` 3個で終わっていることを確認します。
11. 「公開」を押します。
12. 赤いエラーが表示されなければ完了です。

新しいルールでは、学習者の基本情報・最終学習日時に加えて、`skills` 内の6分野を保存できるようになります。

## 2. GitHubへ全ファイルをアップロード

1. ブラウザで、このアプリを公開しているGitHubリポジトリを開きます。
2. リポジトリのファイル一覧上部にある「Add file」を押します。
3. 「Upload files」を選びます。
4. パソコンで `nobiru-kanji-anzan` フォルダを開きます。
5. フォルダ内のファイルをすべて選択し、GitHubのアップロード欄へドラッグします。
6. 同じ名前のファイルは、新しい内容へ置き換わります。
7. 画面下部の変更内容欄に、例として `みっくん冒険メニューと絵カードを改善` と入力します。
8. 「Commit changes」を押します。

今回、特に更新が必要なファイルは次のとおりです。

- `app.js`
- `styles.css`
- `assets/learning-card-atlas-v2.png`
- `assets/learning-card-atlas-extra.png`
- `firebase-sync.js`
- `firebase-config.js`
- `firestore.rules`
- `index.html`
- `version.json`
- `README.md`
- `FIREBASE_SETUP.md`
- `UPDATE_GUIDE.md`

`names.txt` も含め、フォルダ内をすべてアップロードする方法が最も確実です。

## 3. GitHub Pagesの更新を待つ

1. GitHubの「Actions」を開きます。
2. Pagesの処理に緑色のチェックが付くまで待ちます。
3. 通常は1～3分ほどで公開ページへ反映されます。
4. GitHub Pagesの学習アプリを開きます。

## 4. 公開ページで確認

ホーム画面で、次が表示されれば新しい版です。

- 6分野の総合レベル
- 各学習カードに個別の `Lv.`
- 「6つのレベル」一覧
- 「フラッシュカード」と「数字記憶」の学習カード

設定画面では、次を確認します。

1. 「Firebaseと同期済み」または「同期ON」と表示される
2. 「現在の分野別レベル」に6分野が表示される
3. 各分野の「調整」から、その分野だけ開始レベルを変更できる

## 5. 分野別レベルの動作確認

1. 例として「漢字を読む」の開始レベルだけ変更します。
2. ホームへ戻ります。
3. 「漢字を読む」だけが変更され、ほかの5分野が変わっていないことを確認します。
4. 「漢字を読む」を学習し、正解します。
5. 「漢字を読む」のXPだけが増えることを確認します。
6. 別端末で同じGitHub Pagesを開き、同じ名前を選択します。
7. 設定画面の「今すぐ同期する」を押し、6分野のレベルが同じになることを確認します。

## Firestore内の確認方法

Firebaseの「Firestore」→「データ」で、学習後に次の構造が自動作成されます。

```text
nobiru_groups
  └─ nobiru-family-01
      └─ learners
          └─ 学習者ID
              └─ skills
                  ├─ write
                  ├─ read
                  ├─ math
                  ├─ flash
                  ├─ memory
                  └─ digits
```

学習者IDは名前を変換した文字列なので、Firebase上では名前そのものと異なって見える場合があります。

## iPhoneで古い画面が表示される場合

1. Safariの学習アプリのタブを閉じます。
2. GitHub PagesのURLをもう一度開きます。
3. まだ古い場合は、URLの末尾に `?update=30` を付けて一度開きます。
4. 設定画面で「今すぐ同期する」を押します。

この更新では `version.json` の更新番号も変更しているため、通常は保存済みの名前やレベルを消さずに最新版へ切り替わります。

## 「同期できません」と表示される場合

次の順番で確認してください。

1. Firebaseの「ルール」で新しい `firestore.rules` を公開したか
2. GitHubへ `firebase-sync.js` と `firebase-config.js` をアップロードしたか
3. GitHub Pagesの更新処理が完了したか
4. FirebaseとGitHubで同じプロジェクト設定を使っているか
5. インターネットへ接続できているか

Firestoreのコレクションを手作業で作る必要はありません。最初の同期時にアプリが自動作成します。
